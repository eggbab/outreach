services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - ENV=production
    volumes:
      - insta_sessions:/app/insta_sessions
    restart: unless-stopped

volumes:
  insta_sessions:
